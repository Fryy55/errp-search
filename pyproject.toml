[project]
name = "errp-search"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "rich>=14.3.3",
    "typer>=0.24.1",
]

[project.scripts]
errp-search = "errp_search.main:app"

[tool.uv]
package = true